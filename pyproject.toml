[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[project]
name = "create-order-service"
version = "0.1.0"
description = "CreateOrder use case, orchestration, and adapters (hexagonal architecture)"
requires-python = ">=3.10"
dependencies = [
    "sqlalchemy>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]
